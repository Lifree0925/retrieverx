[pytest]
# 让 pytest 把项目根目录加入 sys.path，测试里的 `from app.core... import ...` 才找得到。
# 不写这一行的话，pytest 只会把 tests/unit 加进 sys.path，
# 运行 `pytest tests/unit` 会直接报 ModuleNotFoundError: No module named 'app'。
pythonpath = .
testpaths = tests
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
